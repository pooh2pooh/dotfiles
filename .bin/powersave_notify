#!/bin/bash

current_governor=$(cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor)

if [[ "$current_governor" == "schedutil" ]]; then
  dunstify -a "powersave" -u critical "Режим энергосбережения"
elif [[ "$current_governor" == "powersave" ]]; then
  dunstify -a "powersave" -u critical "Режим производительности"
else
  dunstify -a "powersave" -u critical "Оптимальный режим"
fi

canberra-gtk-play -i power-plug
